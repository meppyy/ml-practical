# Loan Approval Prediction using SVM Classification

To implement a Support Vector Machine (SVM) classification algorithm to classify loan applications as approved (Y) or not approved (N) based on applicant and loan-related attributes.